const fs = require('fs'); const path = require('path'); const testPath = 'data/scenarios/民事案件/民事纠纷材料/接处警登记.txt'; const absolutePath = path.resolve(testPath); console.log(`测试路径: ${testPath}`); console.log(`绝对路径: ${absolutePath}`); try { const stats = fs.statSync(absolutePath); console.log('文件存在!'); console.log(`文件大小: ${stats.size} 字节`); const content = fs.readFileSync(absolutePath, 'utf-8'); console.log(`文件内容长度: ${content.length}`); console.log(`文件内容预览: ${content.substring(0, 100)}...`); } catch (error) { console.error('错误:', error.message); }
